{
  "name": "web-agent-machine",
  "version": "0.2.0",
  "private": true,
  "description": "AI-native machine substrate inside the browser",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter @wam/desktop dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.1"
  }
}
